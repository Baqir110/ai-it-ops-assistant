apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: ai-it-ops
spec:
  selector:
    app: prometheus
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090
  type: ClusterIP
